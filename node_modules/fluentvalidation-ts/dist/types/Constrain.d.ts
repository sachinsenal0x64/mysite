/**
 * Constrain
 * @desc Constrains type `T` to only those properties that exist in type `U`
 */
export declare type Constrain<T, U> = {
    [key in keyof T]: key extends keyof U ? T[key] : never;
};
