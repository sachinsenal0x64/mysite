/// <reference types="node" />
/// <reference types="node" />
import { Stream } from 'stream';
/**
 * Reads the entire request or response body and returns it as received.
 *
 * @param req
 * @return {Promise}
 */
export default function readBody(reqOrRes: Stream): Promise<Buffer>;
