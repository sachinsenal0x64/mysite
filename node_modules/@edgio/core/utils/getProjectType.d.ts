/**
 * Return the type of the project or undefined
 * when property is not set or package.json is not found
 * @returns
 */
export default function getProjectType(projectRoot?: string): string | undefined;
