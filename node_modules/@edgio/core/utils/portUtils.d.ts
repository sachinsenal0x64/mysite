/**
 * Attempts to find the nearest unbound port
 * @param port
 */
export declare function getNearestUnboundPort(port: number): Promise<number | undefined>;
/**
 * Returns true if the port is in use
 * @param port
 * @param host
 */
export declare function isPortBound(port: number, host?: string): Promise<boolean>;
