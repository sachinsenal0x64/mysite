/**
 * Returns the path to root directory of the monorepo
 * or undefined if the current directory is not inside a monorepo
 */
export declare function findMonorepoRoot(currentDir?: string): string | undefined;
