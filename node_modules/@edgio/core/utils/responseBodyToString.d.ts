import Response from '../runtime/Response';
/**
 * Returns the body of the response as a string, unzipping if necessary.
 * @param response
 */
export default function responseBodyToString(response: Response): string;
