"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Converts a path with express-style or regex substitution params
 * to a globby path pattern.
 * @param path
 * @example pathToGlobby('/public/:path*') => '/public/**'
 * @example pathToGlobby('/public/:file.png') => '/public/*.png'
 * @example pathToGlobby('/public/$1') => '/public/**'
 */
function pathToGlobby(path) {
    // Convert unescaped regex substitution '$1' and express style ':path*' param to '**'
    // which matches any number of characters, including /, as long as it's the only thing in a path part
    path = path.replace(/(?<!\\)((?:\\{2})*)(\$[0-9]*)/g, '**');
    path = path.replace(/(:[A-Za-z0-9]+\*)/, '**');
    // Convert express ':path' param to '*'
    // which matches any number of characters, but not /
    return path.replace(/(:[A-Za-z0-9]+)/, '*');
}
exports.default = pathToGlobby;
