import { Conditionals, Boolean } from '../types';
import { ConditionCriteria } from './RouteCriteria';
/**
 * Creates the matching conditions for EdgeControl's `if` property.
 * @param criteria
 * @returns
 */
export declare function createMatchers(criteria: ConditionCriteria): Conditionals | Boolean | null;
