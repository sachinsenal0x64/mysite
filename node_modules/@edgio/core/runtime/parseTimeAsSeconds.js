"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function parseTimeAsSeconds(expression) {
    let value = parseInt(expression.substring(0, expression.length - 1));
    const units = expression[expression.length - 1];
    if (units === 's') {
        return value;
    }
    else if (units === 'm') {
        return value * 60;
    }
    else if (units === 'h') {
        return value * 60 * 60;
    }
    else if (units === 'd') {
        return value * 60 * 60 * 24;
    }
    else {
        throw new Error(`Unsupported time unit "${units}".`);
    }
}
exports.default = parseTimeAsSeconds;
