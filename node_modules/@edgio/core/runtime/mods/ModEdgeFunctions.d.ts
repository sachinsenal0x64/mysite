import Phase from '../Phase';
import Mod from './Mod';
export default class ModEdgeFunctions extends Mod {
    execute(phase: Phase): Promise<void>;
    private set534Response;
    /**
     * Runs the specified edge function.
     * @param edgeFunction
     */
    private run;
    toString(): string;
}
