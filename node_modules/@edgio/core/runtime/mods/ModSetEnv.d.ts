import Mod from './Mod';
import Phase from '../Phase';
/**
 * Emulation of Sailfish's mod_set_env
 */
export default class ModSetEnv extends Mod {
    execute(phase: Phase): Promise<void>;
    private applyUpstreamFeatures;
    private applyHeaders;
    private applyBody;
    toString(): string;
}
