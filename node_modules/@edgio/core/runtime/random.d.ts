/**
 * Returns a random element from the source array
 * @param source
 * @returns
 */
export declare function getRandomElement<TElement, TArray extends Array<TElement>>(source: TArray): TElement;
/**
 * Returns a random integer between 0 and {max}
 * @param max
 * @returns
 */
export declare function getRandomInt(max: number): number;
