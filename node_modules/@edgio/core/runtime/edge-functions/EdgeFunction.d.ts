/// <reference types="node" />
import EdgeFunctionResponse from './EdgeFunctionResponse';
import EdgeFunctionWorkerParams from './EdgeFunctionWorkerParams';
import EdgeFunctionParentInterface from './EdgeFunctionParentInterface';
import EdgeFunctionError from './EdgeFunctionError';
import WasmProgram from './WasmProgram';
export default class EdgeFunction extends WasmProgram {
    private static QuickJsModule;
    private readonly params;
    private readonly parent;
    private response;
    private metrics;
    private readonly DOWNSTREAM_REQUEST_ID;
    private readonly DOWNSTREAM_RESPONSE_ID;
    private panicString?;
    private stdout_written_bytes;
    private stderr_written_bytes;
    constructor(parent: EdgeFunctionParentInterface, params: EdgeFunctionWorkerParams);
    static getQuickJsCore(): Promise<WebAssembly.Module>;
    getPanicString(): string;
    run(wasmInitialMemory?: Buffer): Promise<EdgeFunctionResponse | EdgeFunctionError>;
    funDebug(...args: any[]): void;
    stringLengthsToBuffer(values: string[]): Buffer;
    private getQuickJsImportObject;
}
