import EdgeFunctionWorkerParams from './EdgeFunctionWorkerParams';
export interface EdgeFunctionWorkerData {
    semaphore: Int32Array;
    exchangeBuffer: SharedArrayBuffer;
    exchangeBufferSize: number;
    dataLength: Int32Array;
    params: EdgeFunctionWorkerParams;
}
