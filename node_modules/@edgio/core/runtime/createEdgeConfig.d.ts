import { Router } from '../router';
import PropertyContext from './PropertyContext';
/**
 * Creates the JSON for deploying an environment via the EdgeControl API.
 * @param context The PropertyContext
 * @param router The Router use to create rules
 * @param isEdge set to true if not running in simulator
 * @returns
 */
export default function createEdgeConfig(context: PropertyContext, router: Router): string;
/**
 * Create edge control json object
 * @param context The PropertyContext
 * @param router The Router use to create rules
 * @returns
 */
export declare function createEdgeConfigPropertiesObject(context: PropertyContext, router: Router): {
    rules: import("../types").Rules[];
    name: string;
    hostnames: [import("../types").Hostnames, ...import("../types").Hostnames[]];
    origins: import("../types").Origins[];
    edge_functions: {
        sources: {
            [key: string]: string;
        };
        quickjs_bytecode_base64?: string | undefined;
    } | undefined;
};
