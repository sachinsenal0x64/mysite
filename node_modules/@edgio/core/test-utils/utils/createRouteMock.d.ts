import MockRequest from '../MockRequest';
import MockResponse from '../MockResponse';
import { RequestHeaders } from '../../runtime/Request';
export interface MockRun {
    request: MockRequest;
    response: MockResponse;
}
interface RequestOptions {
    path: string;
    method?: string;
    headers?: RequestHeaders;
    body?: string;
}
/**
 * Creates a mock Request and Response object the router will
 * consume when running the specified request
 * @param options
 * @returns
 */
export default function createRouteMock(options: string | RequestOptions): MockRun;
export {};
