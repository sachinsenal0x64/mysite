"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// @ts-nocheck
class MockResponse {
    constructor(config = {}) {
        this.headers = {
            'x-test': ['jest'],
        };
        this.getHeader = jest.fn(name => this.headers[name.toLowerCase()] || this.headers[name]);
        this.getHeaderValues = jest.fn(); // deprecated
        this.setHeader = jest.fn((name, value) => (this.headers[name] = value));
        this.removeHeader = jest.fn(name => delete this.headers[name]);
        this.getHeaders = () => this.headers;
        this.clone = () => this;
        Object.assign(this, config);
    }
}
exports.default = MockResponse;
